name: Desktop PDV - Manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: windows-latest
    name: Desktop PDV ainda nao publicado
    steps:
      - name: Registrar status operacional
        shell: pwsh
        run: |
          "O Desktop/PDV ainda nao possui projeto oficial publicado neste repositorio." >> $env:GITHUB_STEP_SUMMARY
          "Este workflow permanece manual para nao gerar falhas falsas durante as publicacoes do site e da API." >> $env:GITHUB_STEP_SUMMARY
